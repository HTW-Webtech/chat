<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Chat</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <div class="spread">
    <h1>
      Hallo <%= user %>
    </h1>
    <a href="/logout">logout</a>
  </div>
  <ul class="messages">
    <% messages.forEach(message => { %>
      <li>
        <header class="spread">
          <span class="author"><%= message.user %></span>
          <time><%= new Date(message.date).toLocaleString() %></time>
        </header>
        <p><%= message.message %></p>
      </li>
    <% }); %>
  </ul>
  <form class="message-form" action="/" method="POST" accept-charset="utf-8">
    <input name="message" autofocus>
    <button type="submit">send</button>
  </form>
</body>
</html>
